<div class="flex-1">
  <label for="team_slug" class="text-sm font-medium text-base-content/80 text">
    Subsidiary
  </label>
  <%= form_tag current_team_path, method: :patch, class: "contents", local: true do %>
    <%= hidden_field_tag :status, @status if @status %>
    <%= hidden_field_tag :id, @assessment.id if @assessment %>
    <%= hidden_field_tag :framework, @framework.id if @framework %>
  
    <%= select_tag :team_slug,                                    
        options_from_collection_for_select(
          Current.user.teams.order(:name), 
          :slug, 
          :name, 
          Current.team&.slug
        ),
        class: "select mt-1 w-full",
        onchange: "this.form.submit()" %>
  <% end %>
</div>