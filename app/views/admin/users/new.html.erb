<% content_for(:title) { "Create User" } %>

<div class="h-screen flex">
  <div class="content-center mx-auto">
    <div class="w-full md:w-160 lg:w-160 rounded-lg border-2 border-secondary/10">
    <%= turbo_frame_tag 'create-new-user' do %>
      <h1 class="px-8 py-6 pb-3 text-xl font-semibold">Create User </h1>
      <%= render "form" %>
    <% end %>
  </div>
</div>

