<% content_for(:title) { "Create New Assessment" } %>

<div class="h-screen flex">
  <div class="content-center mx-auto">
    <div class="w-full bg-neutral-100 md:w-160 lg:w-160 rounded-lg border-4 border-secondary/10">
      <%= turbo_frame_tag 'create-new-assessment' do %>
        <h1 class="px-8 py-6 pb-3 text-xl font-semibold">Create Assessment</h1>
        <%= render "form" %>
      <% end %>
    </div>
  </div>
</div>